<h1>All Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <%= link_to contact.name, contact_path(contact.id) %>
    </li>
  <% end %>
</ul>
<%= link_to "Create a new Contact", new_path %>
